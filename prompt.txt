Extract all readable text from the image accurately and present the output in a clean, human-readable format.

Requirements:
1. Preserve the original reading order (left to right, top to bottom).
2. Reconstruct full words correctly (do not split characters or merge unrelated words).
3. Correct obvious OCR character errors (for example: replace misread symbols like ‘8’ → ‘B’, ‘0’ → ‘O’, ‘1’ → ‘l’ where contextually appropriate).
4. Maintain proper spacing between words.
5. Keep punctuation such as commas and periods where they belong.
6. Do not include garbage symbols, random numbers, or OCR noise unless they are clearly part of the original text.
7. Output plain text only — no bounding boxes, no coordinates, no extra metadata.

Final output format:
- Each sentence on its own line.
- Paragraphs separated by a blank line.
- No confidence scores.
- No explanations.

Goal:
Return clean, readable English text exactly as a human would type it after reading the image.
